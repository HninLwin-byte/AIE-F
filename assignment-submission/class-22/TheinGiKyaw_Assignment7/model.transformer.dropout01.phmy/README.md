# Transformer Dropout 0.1 Experiment

New experiment folder. This keeps outputs separate from `model.transformer.phmy`.

Generated files:

- `model.npz`
- `model.npz.best-bleu.npz`
- `train.log`
- `valid.log`
- `valid.ph-my.output`

